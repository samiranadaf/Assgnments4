function joinArray(array) {
    // return array.toString();
    console.log(array.toString());
    console.log(array.join('+'));
    console.log(array.join());
}
joinArray(["red", "green", "orange"]);
